# 案件管理システム 運用マニュアル

ソフトウェア機能追加要求の案件管理システムの使い方をまとめたマニュアルです。
はじめて使う方は「1. はじめに」から順に読んでください。日々の操作だけ知りたい方は「4. 毎日の使い方」へどうぞ。

-----

## 1. はじめに

### このシステムは何をするもの？

顧客や社内からの「機能を追加してほしい」という要求（＝案件）を、登録から出荷まで一元管理するためのシステムです。

- 各担当が案件と進捗をかんたんに登録できる
- 期限切れ・期限間近を自動でアラート表示する
- マネージャーが全案件の状況を一覧で把握できる
- 工場への連絡メールを正しい書式で自動生成できる
- 入力する項目を、自社の運用に合わせて自由に編集できる
- 議事・検討の記録は OneNote に、資料は Box に紐付けられる

### 全体の役割分担

このシステムは、すべてを抱え込むのではなく、各ツールの得意分野を組み合わせて使います。

|役割                       |使うもの                 |
|-------------------------|---------------------|
|案件の一覧・進捗・アラート            |本システム（このアプリ）         |
|議事・検討の記録（ノート）            |SharePoint 上の OneNote|
|資料ファイル（Excel/PPT/メール等）の保存|Box                  |
|案件データの共有・保管              |Box 上の共有 CSV ファイル    |

### 画面の構成（3つのタブ）

|タブ     |内容                         |
|-------|---------------------------|
|案件一覧   |案件の登録・編集・検索、CSVの読み書き、アラート確認|
|ダッシュボード|状況別の件数、要対応案件の一覧（マネージャー向け）  |
|項目管理   |入力項目の追加・編集・並び替え（管理者向け）     |

-----

## 2. 準備（最初に1回だけ）

### アプリの開き方

1. Box の共有フォルダにある `案件管理システム.html` を **Chrome で開く**（Box 上でファイルを開く）
1. アプリ画面が表示される

> **もし Box 上で開いて操作できない（ボタンが反応しない）場合は**、ファイルを自分のパソコンにダウンロードしてから開いてください。お使いの環境により、どちらが動くかが異なります。

> **ポイント:** アプリ本体（HTMLファイル）は表示するだけのものです。案件データは別ファイル（CSV）で共有します。詳しくは「3. データ共有のしくみ」を参照。

### 既存データの取り込み（移行時のみ）

これまで使っていた案件管理表を CSV で書き出し、本システムに取り込めます。手順は「5. CSVの読み込み」を参照してください。列の名前がそろっていなくても「列を指定して読込」で対応づけできます。

-----

## 3. データ共有のしくみ（重要）

### 仕組みのイメージ

このシステムでは、**アプリ本体**と**案件データ**を分けて扱います。

- **アプリ本体（HTMLファイル）** = 「入力画面の説明書」。Box に置いて全員が開く。
- **案件データ（共有CSV）** = 「みんなで書き込む1冊のノート」。Box の共有フォルダに1つだけ置く。

各自がアプリを開いて Box の共有 CSV を読み込み → 編集 → CSV を書き出して Box に戻す、という流れで全員が同じ内容を共有します。

### 共有CSVの置き場所

Box の決められた共有フォルダに、案件データの CSV ファイルを1つ置いて、それを「正（マスター）」とします。

> 例: `Box / 案件管理 / anken_master.csv`

### 大切な約束ごと（必ず守ってください）

データが消えたり食い違ったりするのを防ぐため、次の3つを守ってください。

1. **編集を始める前に、必ず最新の CSV を読み込む**（古い内容に上書きしないため）
1. **編集が終わったら、こまめに CSV を書き出して Box に保存する**（保存し忘れ防止）
1. **同じ案件を同時に複数人で編集しない**（後に保存した人の内容で上書きされるため）

-----

## 4. 毎日の使い方

### 1日の流れ

**① 仕事を始めるとき（読み込み）**

アプリを開くと、画面の上部に「**作業を始める前に、Boxの最新CSVを読み込みましょう**」という案内が表示されます。

1. Box の共有フォルダから最新の CSV をダウンロードする
1. 案内の「**📥 最新CSVを読み込む**」ボタンを押して、その CSV を選ぶ
1. 画面上部に「最後にCSV読込: ◯◯（◯件）」と表示されれば成功

> 前回このPCで保存した内容は自動で復元されますが、それは**自分の手元の状態**です。チームの最新を反映するには、上記のとおり共有CSVの読み込みが必要です。前回の続きを見たいだけのときは「前回の続きで作業する」を押せば案内を閉じられます。

> **注意:** ブラウザの仕組み上、アプリが起動時に勝手にファイルを読み込むことはできません（セキュリティのため、ファイルを開くには必ず人の操作が必要です）。そのため「ボタンを押して読み込む」方式になっています。

**② 案件を登録・更新する**

「5. 操作ガイド」を参照して、案件の追加や進捗の更新を行う。

**③ 区切りのタイミング・仕事を終えるとき（保存）**

1. 画面上部の保存状態が「⚠ 未保存の変更あり」（黄色）になっていることを確認
1. 「**💾 BoxへCSV保存**」ボタン（または「CSV出力」）を押す
1. 書き出された CSV ファイルを、Box の共有フォルダに**上書きアップロード**する
1. 保存状態が「✓ 保存済み」（緑）に戻れば完了

> **画面上部のステータスバーの見方**
> 
> - 緑「✓ 保存済み」= Box に出すべき変更はない状態
> - 黄「⚠ 未保存の変更あり」= まだ Box に保存していない変更がある。CSV を書き出して Box に戻すこと

-----

## 5. 操作ガイド

### 新しい案件を登録する

1. 「**＋ 新規案件を登録**」ボタンを押す
1. **案件名**を入力（必須）
1. 分かる範囲で、要求元・状況・各日程などを入力する
1. 「保存」を押す → **案件IDが自動で採番されます**（例: `A-2026-001`）

> 入力項目はグループ（基本情報・日程・開発・メモ・連絡先）ごとに分かれて表示されます。決まっていない項目は後で追記すればOKです。最初から全部埋める必要はありません。

### 進捗を更新する

1. 一覧から対象の案件をクリック
1. 決まった項目を入力
1. 「保存」を押す

### CSVの読み込み

読み込みボタンは2種類あります。

**「CSV読込」**
本システムから書き出した CSV や、列名がシステムとそろっている CSV を読み込むときに使います。ワンクリックで取り込めます。

**「列を指定して読込」**
別の管理表など、列の名前がシステムと違う CSV を読み込むときに使います。CSV を選ぶと、「システムの項目」と「CSVの列」を対応づける画面が出ます。システムが列名から自動で推測した対応を初期表示するので、違う部分だけプルダウンで直して「この対応で読み込む」を押せば取り込めます。

> どちらの読み込みも、案件IDが入っていない行には自動で採番します。

### CSVの書き出し

「**CSV出力**」を押すと、現在のデータを CSV ファイルとして書き出します。ファイル名には日時が入る（例: `anken_20260611_1430.csv`）ので、Box で上書きしても古い世代が残り、万一のとき遡れます。

-----

## 6. 項目管理（入力項目を自社の運用に合わせる）

「**項目管理**」タブでは、案件の入力項目を自由に編集できます。運用が変わったときも、項目を作り直さずに画面で調整できます。

### できること

- **項目名の変更**：左の欄を書き換える
- **タイプの変更**：自由記入／メモ（複数行）／日付／選択肢／数値 から選ぶ
- **グループの変更**：項目をどのまとまりに表示するか
- **一覧表示の切り替え**：チェックを入れた項目だけ一覧表に列として出る
- **項目の追加**：「＋ 項目を追加」で新しい項目を作る
- **項目の削除**：不要な項目の「削除」を押す
- **並び替え**：左の「≡」をドラッグして順番を変える
- **グループの追加**：「＋ グループを追加」で新しいまとまりを作る

変更は即座にフォームと一覧に反映されます。

### タイプの説明

|タイプ       |用途                           |
|----------|-----------------------------|
|自由記入      |短いテキスト（要求元、バージョンなど）          |
|メモ（複数行）   |長い文章（説明、備考、メモなど）             |
|日付        |カレンダーで日付を選ぶ（納期、出荷日など）        |
|選択肢       |決まった値から選ぶ（状況など）。選択肢はカンマ区切りで設定|
|数値        |数字（工数など）                     |
|案件ID（自動採番）|システムが採番する案件ID。変更・削除はできません    |

### 選択肢の設定

タイプを「選択肢」にすると、下に入力欄が出ます。選べる値をカンマ区切りで入力してください。

> 例: `未着手, 対応中, 完了`

### アラートの設定

タイプを「日付」にすると、「**この日付でアラートを出す（期限切れ／14日以内）**」というチェックが出ます。これをオンにした日付項目は、期限が過ぎたり近づいたりすると、一覧の「調整漏れ」列に警告が表示されます。

> 御社の運用で「ここの期限が漏れると困る」という日付項目にチェックを入れてください。

### 注意

- 「案件名」と「案件ID」は必須項目のため、削除・タイプ変更はできません。
- 項目を削除しても、すでに入力済みのデータ自体は消えません（一覧に表示されなくなるだけです）。
- 「初期項目に戻す」を押すと、項目定義が初期状態に戻ります（案件データは消えません）。

-----

## 7. アラート（期限漏れの通知）

記入漏れや期限切れを防ぐため、システムが自動でアラートを出します。

### アラートが出る条件

- 「項目管理」でアラート対象にした日付項目が、**期限を過ぎている／14日以内に迫っている**
- ただし状況が「完了」の案件にはアラートを出しません

### アラートの見方

- 一覧の「**調整漏れ**」列に ⚠ マークと内容が表示される
- 上部のカードに「要対応」の件数が出る
- フィルタで「**要対応のみ**」を選ぶと、対応が必要な案件だけに絞れる

### ダッシュボード（マネージャー向け）

「**ダッシュボード**」タブを開くと、状況別の件数と、要対応案件の一覧をまとめて確認できます。

-----

## 8. 工場への連絡メールを作成する

人がメール文面を書くのではなく、システムが正しい書式のメールを自動生成します。書式のズレや案件IDの重複を防げます。

### 手順

1. 対象の案件を開く
1. 「連絡先」グループの**宛先(To)**を入力（必要なら CC も）
1. 「保存」を押す
1. 「**📧 メール下書きを作成**」ボタンを押す
1. メールソフトが開き、件名・本文が自動で入った下書きができる
1. **内容を確認してから、自分で送信ボタンを押す**

### 生成されるメールの書式

- **件名:** `[案件] A-2026-001 / 案件名 / 状況`
- **本文:** 各項目を「項目名: 値」の形で並べたもの

> 件名の先頭に付く `[案件]` タグにより、SharePoint 側の自動仕分けの仕組みに乗ります。

> **注意:** メール下書きは標準の `mailto` 方式です。本文が長いと一部の環境で末尾が切れる場合があります。その際は内容を確認のうえ補ってください。

-----

## 9. 記録ノート（OneNote）との連携

議事や検討の記録は、SharePoint 上の OneNote に残し、本システムからリンクで開けるようにします。

### なぜ OneNote を SharePoint 上に置くのか

OneNote を Box に置くと、複数人編集で「最新が参照できない」「古い内容に上書きしてしまう」という事故が起きます。これは Box と OneNote の同期の相性によるものです。**OneNote を SharePoint 上に置くと、Microsoft 純正の同期が効き、この事故が起きません。**

### リンクの設定手順

1. SharePoint 上の OneNote で、その案件用のページを作る
1. ページを右クリック →「**ページへのリンクをコピー**」
1. 本システムで対象の案件を開き、「連絡先」グループの「OneNoteリンク」欄に貼り付けて保存
1. 一覧の「**記録**」列に 📓 ボタンが表示される → クリックでそのページを直接開ける

-----

## 10. 困ったときは（トラブル対処）

**Q. 他の人が登録した案件が見えない**
→ 最新の共有 CSV を読み込んでいない可能性があります。Box から最新 CSV をダウンロードし、「CSV読込」してください。

**Q. 自分の編集が消えた**
→ 編集を CSV に書き出して Box に保存する前に、誰かが古い内容を上書きした可能性があります。「3. データ共有のしくみ」の約束ごとを再確認してください。

**Q. 画面が黄色いまま／「未保存の変更あり」が消えない**
→ まだ Box に保存していない変更があります。「💾 BoxへCSV保存」を押し、書き出した CSV を Box に上書きしてください。

**Q. CSVを読み込んだら一部の項目が空欄になる**
→ CSV の列名がシステムの項目と違う可能性があります。「**列を指定して読込**」を使い、対応づけ画面で正しい列を選んでください。

**Q. 項目を増やしたい／名前を変えたい**
→ 「項目管理」タブで自由に編集できます。「6. 項目管理」を参照してください。

**Q. アプリを閉じたらデータは消える？**
→ 消えません。各自のブラウザに自動保存されます。ただし**チームで共有されるのは Box 上の CSV だけ**なので、共有したい変更は必ず CSV に書き出して Box に保存してください。

-----

## 11. このシステムの位置づけと今後

### 現在の運用（手動同期方式）

各自がアプリを開き、Box の共有 CSV を手動で読み込み・書き出しして共有します。4〜10人程度で、同じ案件を同時に編集することがほぼないチームでの利用を想定しています。サーバーが不要で、すぐに使えるのが利点です。

### 手動同期の限界

- 同じ案件をほぼ同時に編集すると、後に保存した人の内容で上書きされることがあります
- 読み込み・書き出しを手作業で行う必要があります

これらは「編集前に最新を読み込む」「こまめに保存する」「同時編集を避ける」という運用ルールで実用上カバーできます。

### 将来の拡張候補

- 工場連絡メールの**自動下書き作成**（Graph API 連携）
- 案件登録と同時に OneNote ページを**自動作成**してリンクを設定
- 利用人数が増え同時編集が増えた段階での、**サーバー（バックエンド＋データベース）への移行**
- 資料の**自動生成**

いずれも、現在の CSV の項目構成をそのまま引き継いで移行できるよう設計されています。

-----

*このマニュアルは運用に合わせて適宜更新してください。*
